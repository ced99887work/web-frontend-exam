import { createTheme } from '@mui/material/styles';

export const muiTheme = createTheme({
  palette: {
    gray100: {
      main: '#FFFFFF',
    },
    gray700: {
      main: '#999999',
    },
  },
});
